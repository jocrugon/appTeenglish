import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.peruviandeveloper.teenglish',
  appName: 'Teenglish',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
